"use client";
import { useEffect, useState } from "react";

/* === Ton code client actuel de la page admin === */
/* Tu peux coller ici exactement ce que tu avais dans src/app/admin/page.tsx
   à partir de "use client" (composants AdminStats, AdminPage, etc.).
   Exemple minimal pour que ça compile : */

function AdminStats() { return null; } // <-- garde tes vraies implémentations
export default function AdminClient() { return <main className="mx-auto max-w-5xl px-4 py-10">Admin</main>; }

/* Remplace par ton code réel (AdminStats + AdminPage que tu avais) */
